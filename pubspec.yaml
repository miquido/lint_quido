name: miquido_flutter_lints
description: A collection of Flutter/Dart lints that we use in Miquido projects
version: 0.0.0
homepage:

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=2.10.2"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  dart_code_metrics: 4.12.0
  flutter_test:
    sdk: flutter

flutter: